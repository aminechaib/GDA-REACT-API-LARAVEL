# Frontend Design Enhancement TODO (Pure CSS Version)

## Approved Plan Steps (Step-by-step implementation):

1. **[x]** Tailwind install skipped due to CLI issues (using pure CSS enhancements).

2. **[x]** Added dark mode toggle to App.vue (localStorage + CSS vars from base.css).

3. **[x]** Enhanced App.vue template/styles: Gradient header, search icon, responsive layout, striped table rows, hover effects.

4. **[x]** Updated src/assets/main.css: Override scaffold layout.

5. **[ ]** Test: Run `cd frontend && npm run dev` in terminal, verify responsive design, dark mode toggle, loading spinner, API data, mobile view (DevTools).

6. **[ ]** Optional: Extract Header to Header.vue.

*Progress updated after each step.*
